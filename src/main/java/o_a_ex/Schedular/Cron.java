package o_a_ex.Schedular;
import lombok.extern.slf4j.Slf4j;
import o_a_ex.Entities.Utilisateur;
import o_a_ex.Repositories.RepoUtilisateur;

import java.util.List;

@Slf4j
public class Cron {
    /*
@Scheduled(cron = "0/60 * * * * *")
public void afficher() {
   List<Utilisateur> x= RepoUtilisateur
    for (Reservation res : reservationRepo.findAll()) {
       if (res.getEtatReservation()== EtatReservation.ENCOURS) {
           res.setEtatReservation(EtatReservation.ANNULEE);
           reservationRepo.save(res);
           log.info("Reservation annulé:"+res.getIdReservation());
       }
   }
}

    */
}
